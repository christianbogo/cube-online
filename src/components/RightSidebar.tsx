export default function RightSidebar() {
    return (
        <aside className="h-full bg-bg-secondary w-full select-none flex items-center justify-center text-text-secondary text-sm">
            {/* Blank for now */}
            Right Sidebar
        </aside>
    );
}
